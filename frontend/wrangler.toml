# Cloudflare Pages config for the Vidia Open Studio frontend.
# Pages reads pages_build_output_dir to locate the Vite build output (relative
# to the Pages "Root directory" setting, which is this frontend/ folder).
name = "vidia-app"
compatibility_date = "2024-10-22"
pages_build_output_dir = "dist"
